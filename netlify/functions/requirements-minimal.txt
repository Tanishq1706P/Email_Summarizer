# Minimal Netlify Functions requirements (wheel-friendly, no Rust/build-heavy deps)
# Core API runtime only
fastapi>=0.100.0
uvicorn[standard]>=0.20.0
pydantic>=2.0
python-multipart
python-dotenv
email-validator
redis>=4.0.0
mangum>=0.17

# Optional (add if used in api.py without heavy subdeps)
# beautifulsoup4
# bleach
